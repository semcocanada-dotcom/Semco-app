export const BUILD_VERSION = '1.0.3';
export const BUILD_NUMBER = '4';
export const BUILD_LABEL = `Preview ${BUILD_VERSION} (${BUILD_NUMBER})`;
export const BUILD_NOTE = 'Color and bottom-nav fix build';
