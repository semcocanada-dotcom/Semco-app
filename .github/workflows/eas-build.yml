name: EAS — Native Build

on:
  workflow_dispatch:
  push:
    branches:
      - semco-pro-preview
    paths:
      - 'BUILD_TRIGGER'

permissions:
  contents: read

jobs:
  build-preview:
    name: Build iOS Preview (Native)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Preflight Expo auth
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: |
          if [ -z "$EXPO_TOKEN" ]; then
            echo "EXPO_TOKEN secret is missing or empty. Add it in GitHub repo settings before running this workflow."
            exit 1
          fi
          echo "EXPO_TOKEN is set; continuing."

      - name: Build iOS preview
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: npx -y eas-cli@latest build --platform ios --profile preview --non-interactive --clear-cache
